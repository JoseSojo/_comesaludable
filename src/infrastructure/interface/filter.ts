export type Filters = {
  name?: string;
  category?: string;   // fecha máxima
  type?: string;   // fecha máxima
  environment?: string;   // fecha máxima
  param?: string;   // fecha máxima
  restaurant?: string; // Restaurante (para menus, comentarios y eso)
};